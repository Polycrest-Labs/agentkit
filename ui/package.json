{
  "name": "@polycrestlabs/agentkit-ui",
  "version": "0.2.0",
  "description": "The AgentKit conversation UI kit for Angular: SSE frame parsing, the agent-turn state machine (heartbeat liveness, dead-air timeout, refusals, attachment gating), the chat rail, and the transport/confirm injection seams. Types the loaded · suggestion · changeset · heartbeat · error frame vocabulary as its core event union.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/Polycrest-Labs/agentkit.git",
    "directory": "ui"
  },
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "ng-packagr -p ng-package.json -c tsconfig.lib.json",
    "test": "vitest run"
  },
  "peerDependencies": {
    "@angular/common": "^22.0.0",
    "@angular/core": "^22.0.0",
    "rxjs": "^7.8.0"
  },
  "dependencies": {
    "marked": "^18.0.6",
    "tslib": "^2.3.0"
  },
  "devDependencies": {
    "@angular/common": "^22.0.0",
    "@angular/compiler": "^22.0.0",
    "@angular/compiler-cli": "^22.0.0",
    "@angular/core": "^22.0.0",
    "jsdom": "^28.0.0",
    "ng-packagr": "^22.0.0",
    "rxjs": "~7.8.0",
    "typescript": "~6.0.2",
    "vitest": "^4.1.10"
  }
}
